version: '3'
services:
  redis:
    container_name: redis-db
    image: redis:6-alpine
    restart: on-failure
    command: redis-server /etc/redis/redis.conf --appendonly yes    
    ports:
      - 6379:6379
    volumes:
      - ./data:/data
      - ./conf:/etc/redis
